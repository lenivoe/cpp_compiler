// тест для синтаксического анализатора без ошибок

class _1a55 {
    int a, b, c[03], d[4], e = ++t++;
};

short int gNum8 = 001234567;
long gNum10 = 89, gArray[0XabcDEF];
int gNum16 = 0x123456789ABCdef;
char while77while;
_1a55 _1, _2 = ((_1));

int main() { }
int main() {
    while77while = -1 == 4 != 5;
    while (while77while) {
        {{{}{}{}}}
        while (gArray[1]) while (1) ;;;
        char f;
        m.m[f == 0 <= 1 >= 01 > (0xF) < f]
            .m[f << f >> f + f - f * f]
            .m[f++ / --f-- % --(++f)]= f;
    }
    return _2.x * (-1 / +2);
}

class Ca {
    class main__ {
        class Ca {};
        class Ca {};
    };
};